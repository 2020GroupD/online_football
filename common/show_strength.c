#include "head.h"

struct Map court;
WINDOW *Write;
int sockfd;

void show_strength() {
	char tmp;
	int len = 0;
	struct Ctl ctll

	while (scanf("%s", &tmp) != 'k') {
		if (tmp == ' ') break;
		len = (len + 1)%20;
		wmove(Write, 6, len);
	}
	if ()


}
